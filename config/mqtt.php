<?php

return [

    /*
    |--------------------------------------------------------------------------
    | MQTT Broker Host
    |--------------------------------------------------------------------------
    */
    'host' => env('MQTT_HOST', '92aa29cd2ae840bc921a75aa98c2bc66.s1.eu.hivemq.cloud'),

    /*
    |--------------------------------------------------------------------------
    | MQTT Broker Port
    --------------------------------------------------------------------------
    */
    'port' => env('MQTT_PORT', 8883),

    /*
    |--------------------------------------------------------------------------
    | MQTT Client ID
    |--------------------------------------------------------------------------
    */
    'client_id' => env('MQTT_CLIENT_ID', 'laravel-easypark'),

    /*
    |--------------------------------------------------------------------------
    | MQTT Authentication
    |--------------------------------------------------------------------------
    */
    'username' => env('MQTT_USERNAME', 'Chandra'),
    'password' => env('MQTT_PASSWORD', 'Chandra13July'),

    /*
    |--------------------------------------------------------------------------
    | MQTT Quality of Service (QoS)
    | 0 = at most once
    | 1 = at least once (RECOMMENDED)
    | 2 = exactly once
    |--------------------------------------------------------------------------
    */
    'qos' => env('MQTT_QOS', 1),

    /*
    |--------------------------------------------------------------------------
    | Connection Timeout (seconds)
    |--------------------------------------------------------------------------
    */
    'timeout' => env('MQTT_TIMEOUT', 5),

    /*
    |--------------------------------------------------------------------------
    | Use TLS / SSL (true/false)
    | Gunakan true jika pakai HiveMQ Cloud Private port 8883
    |--------------------------------------------------------------------------
    */
    'use_tls' => env('MQTT_USE_TLS', false),
];
